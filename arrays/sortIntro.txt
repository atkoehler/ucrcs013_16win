sort this

5 3 2 1 7 5

array size stored in: const int SIZE;


// Bubble sort attempt - N*N time
for ( i=0; i< SIZE; i++ ) {
    for ( j=0; j < SIZE, j++) {
        if (arr[i] > arr[j]) {
        // swap
        }
    }
}
    
// original
        if (arr[i] > arr[i+1+j]) {
            arr[i]=temp;
            arr[i]=arr[i+1+j];
            arr[i+1+j]=temp;
        }
    }
}